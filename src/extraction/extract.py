"""
extract.extract()
Puerto del lambda actual: debe devolver un CSV temporal con las columnas esperadas.
Implementación pendiente: por ahora devuelve None.
"""
from typing import Optional

def extract(output_path: str) -> Optional[str]:
    """
    Ejecutar scraping y guardar resultado en CSV en output_path.
    Retornar la ruta al CSV o None si falla.
    """
    # TODO: migrar lógica desde lambda.py aquí
    return None
